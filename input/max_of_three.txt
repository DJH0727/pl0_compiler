var a, b, c, max;

procedure findMax;
begin
    max := a;
    if b > max then
        max := b;
    if c > max then
        max := c;
end;

begin
    read (a, b, c);
    call findMax;
    write (max);
end.
